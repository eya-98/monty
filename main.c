#include "monty.h"
/**
 *
 *
 *
 */
int main(int argc, char **argv)
{
stack_t *head, g_head;
if (argc != 2)
{
printf("USAGE: monty file\n");
exit(EXIT_FAILURE)
}
*head = NULL
g_head = &head: 
r = _read(argv[1], &head);
exit(0)  
}
